代理协议的书写步骤。。
1.在某个类的头文件上声明@protocol协议（协议名，协议中的方法）
2.在这个类中声明了一个遵守协议的对象属性（属性关键字用assign，遵守协议对象的类型是id类型，因为不确定那个对象会遵循协议）
3.在这个类中设定协议触发的时机（操作），，在触发协议之前，需要先进行判断，判断遵循协议的对象是否存在，遵循协议的对象是否响应协议里必须实现的方法




4.在遵循协议的类的头文件中引入设置协议类的头文件，并且在当前类的父类后面用<>加协议名，表示当前类遵循这个协议
5.实现协议中的方法，做自己的操作
6.在遵循协议的类中设置代理